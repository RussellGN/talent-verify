import { Typography } from "@mui/material";

export default function page() {
   return (
      <div className="min-h-[75vh] flex items-center justify-center">
         <Typography className="text-center p-4">This page does not exist</Typography>
      </div>
   );
}
